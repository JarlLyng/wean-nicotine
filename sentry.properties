# Sentry CLI configuration for source-map uploads during build.
# Auth token is read from SENTRY_AUTH_TOKEN env var (set in .env or shell).
# See docs/SENTRY.md for setup details.

defaults.org=SET_YOUR_ORG
defaults.project=SET_YOUR_PROJECT
